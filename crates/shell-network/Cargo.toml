[package]
name = "shell-network"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
shell-consensus = { path = "../shell-consensus" }
shell-crypto = { path = "../shell-crypto" }
shell-mempool = { path = "../shell-mempool" }
shell-primitives = { path = "../shell-primitives" }

[dev-dependencies]
shell-fixtures = { path = "../shell-fixtures" }
